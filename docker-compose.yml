services:
  web:
    build: .
    ports:
      - "${WEB_PORT:-8080}:80"

  verify:
    build:
      context: .
      dockerfile: Dockerfile.verify
    depends_on:
      - web
    environment:
      PLAYWRIGHT_BASE_URL: http://web:80
